import { HOME_GETTAG } from './constants'

const initState = {}

export default (state = initState, actions) => {
    switch (actions.type) {
        case HOME_GETTAG:

            break;

        default:
            return state
    }
}